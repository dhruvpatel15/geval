# Multi-policy contract demonstration.
# This contract pulls in both quality and safety rules.
name: multi-policy-demo
version: "1.1.0"
combine: all_pass
policies:
  - path: policies/quality.yaml
  - path: policies/safety.yaml
